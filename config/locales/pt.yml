pt:
  # Attributes names common to most models
  #attributes:
    #created_at: "Created at"
    #updated_at: "Updated at"

  # Active Record models configuration
  activerecord:
    errors:
      messages:
        taken: "já existe"
        record_invalid: "Falha na validação: %{errors}"
        restrict_dependent_destroy:
          one: "Cannot delete record because a dependent %{record} exists"
          many: "Cannot delete record because dependent %{record} exist"
      models:
          orgao:
              attributes:
                  nome:
                      blank: "não foi informado"
                  url:
                      blank: "não foi informado"
                  siorg:
                      blank: "não foi informado"
        # Append your own errors here or at the model/attributes scope.

      # You can define own errors for models or model attributes.
      # The values :model, :attribute and :value are always available for interpolation.
      #
      # For example,
      #   models:
      #     user:
      #       blank: "This is a custom blank message for %{model}: %{attribute}"
      #       attributes:
      #         login:
      #           blank: "This is a custom blank message for User login"
      # Will define custom blank validation message for User model and
      # custom blank validation message for login attribute of User model.
      #models:

    # Translate model names. Used in Model.human_name().
    models:
        orgao: "Órgão"
        dataset: "Dataset"
        

    # Translate model attribute names. Used in Model.human_attribute_name(attribute).
    attributes:
        orgao:
            nome: 
                one: "Nome"
                other : "Nomes"
            url: "URL"
            siorg: "Código SIORG"
            licenca: "Licença"
            url_sobre: "URL da Página Sobre"
            url_competencias: "URL das Competências"
            url_historico: "URL do Histórico"
            url_organograma: "URL do Organograma"
            url_quem_e_quem: "URL do Quem é Quem"

