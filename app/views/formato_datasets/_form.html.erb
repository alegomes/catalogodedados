<%= form_for(@formato_dataset) do |f| %>
  <% if @formato_dataset.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@formato_dataset.errors.count, "error") %> prohibited this formato_dataset from being saved:</h2>

      <ul>
      <% @formato_dataset.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nome %><br />
    <%= f.text_field :nome %> (obrigatório)
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
