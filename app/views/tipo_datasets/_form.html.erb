<%= form_for(@tipo_dataset) do |f| %>
  <% if @tipo_dataset.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tipo_dataset.errors.count, "error") %> prohibited this tipo_dataset from being saved:</h2>

      <ul>
      <% @tipo_dataset.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nome %><br />
    <%= f.text_field :nome %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
