<%= form_for(@licenca) do |f| %>
  <% if @licenca.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@licenca.errors.count, "error") %> foram encontrados:</h2>

      <ul>
      <% @licenca.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nome %><br />
    <%= f.text_field :nome %> (obrigatório)
  </div>
  <div class="field">
    <%= f.label :url %><br />
    <%= f.text_field :url %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
