require 'spec_helper'

# This spec was generated by rspec-rails when you ran the scaffold generator.
# It demonstrates how one might use RSpec to specify the controller code that
# was generated by Rails when you ran the scaffold generator.
#
# It assumes that the implementation code is generated by the rails scaffold
# generator.  If you are using any extension libraries to generate different
# controller code, this generated spec may or may not pass.
#
# It only uses APIs available in rails and/or rspec-rails.  There are a number
# of tools you can use to make these specs even more expressive, but we're
# sticking to rails and rspec-rails APIs to keep things simple and stable.
#
# Compared to earlier versions of this generator, there is very limited use of
# stubs and message expectations in this spec.  Stubs are only used when there
# is no simpler way to get a handle on the object needed for the example.
# Message expectations are only used when there is no simpler way to specify
# that an instance is receiving a specific message.

describe FormatoDatasetsController do

  # This should return the minimal set of attributes required to create a valid
  # FormatoDataset. As you add validations to FormatoDataset, be sure to
  # update the return value of this method accordingly.
  def valid_attributes
    {}
  end
  
  # This should return the minimal set of values that should be in the session
  # in order to pass any filters (e.g. authentication) defined in
  # FormatoDatasetsController. Be sure to keep this updated too.
  def valid_session
    {}
  end

  describe "GET index" do
    it "assigns all formato_datasets as @formato_datasets" do
      formato_dataset = FormatoDataset.create! valid_attributes
      get :index, {}, valid_session
      assigns(:formato_datasets).should eq([formato_dataset])
    end
  end

  describe "GET show" do
    it "assigns the requested formato_dataset as @formato_dataset" do
      formato_dataset = FormatoDataset.create! valid_attributes
      get :show, {:id => formato_dataset.to_param}, valid_session
      assigns(:formato_dataset).should eq(formato_dataset)
    end
  end

  describe "GET new" do
    it "assigns a new formato_dataset as @formato_dataset" do
      get :new, {}, valid_session
      assigns(:formato_dataset).should be_a_new(FormatoDataset)
    end
  end

  describe "GET edit" do
    it "assigns the requested formato_dataset as @formato_dataset" do
      formato_dataset = FormatoDataset.create! valid_attributes
      get :edit, {:id => formato_dataset.to_param}, valid_session
      assigns(:formato_dataset).should eq(formato_dataset)
    end
  end

  describe "POST create" do
    describe "with valid params" do
      it "creates a new FormatoDataset" do
        expect {
          post :create, {:formato_dataset => valid_attributes}, valid_session
        }.to change(FormatoDataset, :count).by(1)
      end

      it "assigns a newly created formato_dataset as @formato_dataset" do
        post :create, {:formato_dataset => valid_attributes}, valid_session
        assigns(:formato_dataset).should be_a(FormatoDataset)
        assigns(:formato_dataset).should be_persisted
      end

      it "redirects to the created formato_dataset" do
        post :create, {:formato_dataset => valid_attributes}, valid_session
        response.should redirect_to(FormatoDataset.last)
      end
    end

    describe "with invalid params" do
      it "assigns a newly created but unsaved formato_dataset as @formato_dataset" do
        # Trigger the behavior that occurs when invalid params are submitted
        FormatoDataset.any_instance.stub(:save).and_return(false)
        post :create, {:formato_dataset => {}}, valid_session
        assigns(:formato_dataset).should be_a_new(FormatoDataset)
      end

      it "re-renders the 'new' template" do
        # Trigger the behavior that occurs when invalid params are submitted
        FormatoDataset.any_instance.stub(:save).and_return(false)
        post :create, {:formato_dataset => {}}, valid_session
        response.should render_template("new")
      end
    end
  end

  describe "PUT update" do
    describe "with valid params" do
      it "updates the requested formato_dataset" do
        formato_dataset = FormatoDataset.create! valid_attributes
        # Assuming there are no other formato_datasets in the database, this
        # specifies that the FormatoDataset created on the previous line
        # receives the :update_attributes message with whatever params are
        # submitted in the request.
        FormatoDataset.any_instance.should_receive(:update_attributes).with({'these' => 'params'})
        put :update, {:id => formato_dataset.to_param, :formato_dataset => {'these' => 'params'}}, valid_session
      end

      it "assigns the requested formato_dataset as @formato_dataset" do
        formato_dataset = FormatoDataset.create! valid_attributes
        put :update, {:id => formato_dataset.to_param, :formato_dataset => valid_attributes}, valid_session
        assigns(:formato_dataset).should eq(formato_dataset)
      end

      it "redirects to the formato_dataset" do
        formato_dataset = FormatoDataset.create! valid_attributes
        put :update, {:id => formato_dataset.to_param, :formato_dataset => valid_attributes}, valid_session
        response.should redirect_to(formato_dataset)
      end
    end

    describe "with invalid params" do
      it "assigns the formato_dataset as @formato_dataset" do
        formato_dataset = FormatoDataset.create! valid_attributes
        # Trigger the behavior that occurs when invalid params are submitted
        FormatoDataset.any_instance.stub(:save).and_return(false)
        put :update, {:id => formato_dataset.to_param, :formato_dataset => {}}, valid_session
        assigns(:formato_dataset).should eq(formato_dataset)
      end

      it "re-renders the 'edit' template" do
        formato_dataset = FormatoDataset.create! valid_attributes
        # Trigger the behavior that occurs when invalid params are submitted
        FormatoDataset.any_instance.stub(:save).and_return(false)
        put :update, {:id => formato_dataset.to_param, :formato_dataset => {}}, valid_session
        response.should render_template("edit")
      end
    end
  end

  describe "DELETE destroy" do
    it "destroys the requested formato_dataset" do
      formato_dataset = FormatoDataset.create! valid_attributes
      expect {
        delete :destroy, {:id => formato_dataset.to_param}, valid_session
      }.to change(FormatoDataset, :count).by(-1)
    end

    it "redirects to the formato_datasets list" do
      formato_dataset = FormatoDataset.create! valid_attributes
      delete :destroy, {:id => formato_dataset.to_param}, valid_session
      response.should redirect_to(formato_datasets_url)
    end
  end

end
